/* import React from "react";

const TextInput = ({ label, onChange, placeholder }) => {
  return (
    <div>
      <label>{label}</label>
      <input onChange={onChange} placeholder={placeholder}></input>
    </div>
  );
};
export default TextInput;
  */
